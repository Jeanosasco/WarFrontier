{
  "name": "warfrontier-studio-ai",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @studio-ai/dashboard",
    "build": "npm run build --workspace @studio-ai/dashboard",
    "lint": "npm run lint --workspace @studio-ai/dashboard",
    "typecheck": "npm run typecheck --workspace @studio-ai/dashboard"
  },
  "engines": {
    "node": ">=20"
  }
}
